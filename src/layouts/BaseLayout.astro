---
import "../styles/base.css";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  noindex?: boolean;
  type?: "website" | "article";
  publishedTime?: string;
  modifiedTime?: string;
  tags?: string[];
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  enableCommandTyping?: boolean;
}

const {
  title = "Personal Notes",
  description = "A minimal personal blog for notes, projects, ideas, and the work behind them.",
  image = "/og-default.svg",
  noindex = false,
  type = "website",
  publishedTime,
  modifiedTime,
  tags = [],
  jsonLd = [],
  enableCommandTyping = false
} = Astro.props;

const pathName = Astro.url.pathname;
const siteUrl = Astro.site ?? new URL(Astro.url.origin);
const canonicalUrl = new URL(pathName, siteUrl).toString();
const imageUrl = new URL(image, siteUrl).toString();
const githubUrl = "https://github.com/gsisinna";
const linkedInUrl = "https://www.linkedin.com/in/gabriele-sisinna-4a6081109/";
const rssUrl = new URL("/rss.xml", siteUrl).toString();
const twitterCard = "summary_large_image";
const jsonLdItems = Array.isArray(jsonLd) ? jsonLd : [jsonLd];
const currentYear = new Date().getFullYear();
const pageKeywords = [...new Set(["robotics software", "C++", "Python", "software architecture", ...tags])]
  .filter(Boolean)
  .join(", ");
const csp = [
  "default-src 'self'",
  "base-uri 'self'",
  "object-src 'none'",
  "form-action https://formspree.io",
  "img-src 'self' https: data:",
  "font-src 'self' https://fonts.gstatic.com data:",
  "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
  "script-src 'self' 'unsafe-inline'",
  "connect-src 'self'",
  "upgrade-insecure-requests"
].join("; ");
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content={pageKeywords} />
    <meta name="robots" content={noindex ? "noindex, nofollow" : "index, follow"} />
    <meta name="googlebot" content={noindex ? "noindex, nofollow" : "index, follow, max-image-preview:large"} />
    <meta name="author" content="Gabriele Sisinna" />
    <meta name="google-site-verification" content="xqHM9Pytu8Nzeeh02WqNgvoUIfhEfE6DkoH_TF-TZ98" />
    <meta name="theme-color" content="#090b0e" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    <meta http-equiv="Content-Security-Policy" content={csp} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="alternate" type="application/rss+xml" title="gsisinna notes RSS" href="/rss.xml" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:site_name" content="gsisinna" />
    <meta property="og:type" content={type} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:image:alt" content="gsisinna personal blog preview" />
    <meta property="og:locale" content="en_US" />
    {publishedTime && <meta property="article:published_time" content={publishedTime} />}
    {modifiedTime && <meta property="article:modified_time" content={modifiedTime} />}
    {type === "article" && tags.map((tag) => <meta property="article:tag" content={tag} />)}
    <meta name="twitter:card" content={twitterCard} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <meta name="twitter:image:alt" content="gsisinna personal blog preview" />
    <link rel="dns-prefetch" href="//fonts.googleapis.com" />
    <link rel="dns-prefetch" href="//fonts.gstatic.com" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=JetBrains+Mono:wght@400;500;700&display=swap"
      rel="preload"
      as="style"
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=JetBrains+Mono:wght@400;500;700&display=swap"
        rel="stylesheet"
      />
    </noscript>
    {jsonLdItems.filter(Boolean).map((item) => (
      <script
        is:inline
        type="application/ld+json"
        set:html={JSON.stringify(item).replace(/</g, "\\u003c")}
      />
    ))}
    <!-- `frame-ancestors` is ignored in meta CSP. Enforce it via HTTP headers at the host/CDN. -->
    <script is:inline>
      if (window.top !== window.self) {
        document.documentElement.style.display = "none";
        try {
          window.top.location = window.self.location.href;
        } catch {}
      }
      document.documentElement.classList.add("js");
    </script>
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to content</a>
    <div class="shell">
      <div class="frame">
        <header class="site">
          <a class="brand" href="/">
            <span class="traffic" aria-hidden="true">
              <span></span>
              <span></span>
              <span></span>
            </span>
            <span class="brand-mark">/</span>
            <span class="brand-copy">
              <strong>gsisinna</strong>
              <span>~ /home/gsisinna/blog</span>
            </span>
          </a>
          <nav aria-label="Primary">
            <a href="/" aria-current={pathName === "/" ? "page" : undefined}>home</a>
            <a href="/search/" aria-current={pathName.startsWith("/search") ? "page" : undefined}>
              search
            </a>
            <a
              href="/journal/"
              aria-current={pathName.startsWith("/journal") ? "page" : undefined}
            >
              notes
            </a>
            <a href="/tags/" aria-current={pathName.startsWith("/tags") ? "page" : undefined}>
              tags
            </a>
            <a href="/contact/" aria-current={pathName.startsWith("/contact") ? "page" : undefined}>
              contact
            </a>
          </nav>
        </header>
        <main id="main-content">
          <slot />
        </main>
        <footer class="site">
          © {currentYear} Gabriele Sisinna. All rights reserved.
          <div class="footer-links" aria-label="Footer links">
            <a href={rssUrl}>RSS</a>
            <a href={githubUrl} target="_blank" rel="noreferrer noopener">GitHub</a>
            <a href={linkedInUrl} target="_blank" rel="noreferrer noopener">LinkedIn</a>
            <a href="/search/">Search</a>
            <a href="/privacy/">Privacy</a>
            <a href="/terms/">Terms</a>
          </div>
        </footer>
      </div>
    </div>
    {enableCommandTyping && <script is:inline src="/scripts/command-typing.js" defer></script>}
  </body>
</html>
